# 开发日志

> 考核要求：每天记录 bug、排查过程、解决方案。Git 提交配合日志一起做。

## 2026-08-15

### 今日进度
- [x] CubeMX 生成 FreeRTOS 工程（F103ZET6 / 72MHz / FREERTOS 中间件），Keil 编译下载跑通
- [x] LED 在默认任务中闪烁（RTOS 调度、vTaskDelay 验证通过）
- [x] 正点原子 ATK-MD0280 例程（FSMC 2.8 寸屏）下载验证，屏幕硬件正常（红字三行 + 3D 旋转立方体 + 触摸拖动）
- [x] LCD 驱动移植进工程：`Drivers/BSP/ATK_MD0280/`（atk_md0280.c/h、fsmc、font），触摸关闭（`USING_TOUCH=0`），工程编译通过
- [x] 屏幕点亮：白底 + 红色 "Hello RTOS!"，LED 慢闪 = 初始化成功
- [x] 建 Git 仓库，初始提交

### 设计决策
- 输入设备用摇杆（双轴 ADC + SW 按键），**触摸功能砍掉**（`ATK_MD0280_USING_TOUCH = 0`），输入只走摇杆
- 光标**大小和灵敏度参数化**：`sys_config_t` 结构体（cursor_size / cursor_sens 默认值 3 / 5），灵敏度用在摇杆→步长换算处，大小用在光标绘制函数——为阶段 4 设置应用预留，实现"改结构体即生效"；跨任务共享加锁推迟到设置任务出现时

## 2026-08-17

### 今日进度
- [x] 光标形状 8 轮迭代定稿：16×18 位图 + 8 邻域膨胀白色描边（Windows 经典箭头风格：38° 斜角、底部横封、尾部加长），尺寸可参数放大（s×s 方块）
- [x] 输入系统完成：`input_task`（15ms 采样、死区 ±100、灵敏度换算 `(delta*sens)/512`、SW 边沿检测）+ 事件队列 `g_event_queue`（容量 8）+ `ui_task`（阻塞消费 → `cursor_move`）
- [x] 摇杆引脚迁移：PA2/PA3 → **PA4(ADC1_IN4) / PA5(ADC2_IN5) / PA6(SW)**。原因：PA0 被板载 WK_UP 占用（10k 上拉影响 ADC 读数）、PA1 接板载电位器；C 代码零改动（引脚全部走宏，CubeMX 重新生成自动更新）
- [x] **修复 postgen.ps1"等长替换"陷阱（重点 bug）**：替换串 41 字节 vs 原串 38 字节，多出的 3 字节覆盖了下一行的 `\r\n` 和 `#`，把 SRAM/TIM 两行合并成一条脏行 → `HAL_TIM_MODULE_ENABLED` 丢失（TIM6 是 HAL 基准时钟，SYS Timebase=TIM6）→ `TIM_HandleTypeDef` 未定义编译报错。修复：改为纯文本级替换 + 脏行自愈 + TIM 宏一并管理
- [x] freertos.c 中文注释被 CubeMX 按系统 ANSI 重写 → 乱码且无法自动还原；改为英文注释（此文件 CubeMX 每次生成都会重写，英文免疫编码问题；App/ 下自有代码保留中文）
- [x] 摇杆验证通过：光标移动方向正确、手感良好，SW 按键事件边沿检测就绪
- [x] 桌面框架 v1 完成：`desktop.c` 状态机（BOOT 启动画面 2 秒 → LOGIN 密码键盘 → DESKTOP 桌面图标），4 位密码（默认 1234）、错误提示、连续错 3 次锁定 30 秒（倒计时期间输入全部无效）；键盘高亮由光标位置驱动（局部重绘旧格+新格）
- [x] **修复 "cannot read project file"（重点 bug）**：加完登录画面后 Keil 打开工程报错、工程树为空，UV4 命令行编译退出码 15 且不写日志。排查：uvprojx/uvoptx 均通过 XML 解析、desktop.c 条目位置正确、无 Keil 进程占用——文件侧全部验证通过仍复现；最后 `git diff` 对照"编译通过的 HEAD 版本"，发现唯一可疑差异 = 工程文件第一行多了一个 UTF-8 BOM。剥离 BOM 后 UV4 命令行编译通过（0 Error / 0 Warning）。**根因：uVision4 5.24 的老解析器读不了带 BOM 的 uvprojx**。已固化进 postgen.ps1 2c 节兜底剥离
- [x] **修复光标白块覆盖 bug**：`cursor_erase` 用白色矩形填充整个光标外框来"擦除"，光标路过文字/图标/键盘格会留下白块。改为**无损恢复**：新增 `atk_md0280_read_area`（0x2E 读 GRAM，每像素 2 次读拼 RGB565）+ `atk_md0280_write_area`（0x2C 连续写）批量读写；光标移动时先把旧位置外框"整块读回→整块写回"恢复背景，再画新位置。缓冲固定 8 行 × 屏宽（3.75KB .bss），分块循环适配任意光标大小（1~8）
- [x] **发现并修复"所有中文不显示"bug**：`atk_md0280_show_string` 的循环条件是 `(*str >= ' ') && (*str <= '~')`——**库只支持 ASCII**，遇到中文直接退出不画。"请输入密码/密码错误/图标名"等全部从未显示过（"密码错误"只有白底擦除生效）。**决策：界面文案全部改英文**（Enter Password / Wrong Password / Locked Ns / Starting... / Files / Paint / Music / Settings），避免引入 GB2312 字库依赖
- [x] 顺带修正：hint 行擦除区 55→47（原擦到 y=52 起的密码圆点）；键盘格高亮重绘后 `cursor_show()` 重画光标（格子会盖住光标，保证光标永远最上层）；图标名按 strlen 动态居中

### 设计决策
- 屏幕渲染独占（单写者原则）：所有 LCD 绘制只在 `ui_task`，其他任务禁止碰屏幕，避免并发抢 FSMC 总线导致画面撕裂
- 事件驱动架构：`ui_task` 用 `xQueueReceive(..., portMAX_DELAY)` 永久阻塞，无事件时完全让出 CPU；生产（input_task）与消费（ui_task）通过队列解耦
- 固化 CubeMX 重生成流程：改 `.ioc` → GENERATE CODE → 双击 `postgen.bat` → 编译，任何一步异常先检查实际生成的文件内容，别只信"脚本看起来对"
- 引脚宏化设计兑现价值：换引脚只需改 CubeMX，C 代码零改动
- **文件编码铁律**：uvprojx 必须是**无 BOM 的 UTF-8**（Keil4 不认带 BOM 的工程文件）；hal_conf.h 等纯 ASCII 文件用 ASCII 编码写；只有带中文的源文件才用 UTF-8 带 BOM（Keil+VS Code 双兼容）。postgen 写入时必须显式确认编码，别想当然"UTF-8 就行"
- **界面文案用英文**：atk_md0280 库 show_string 只支持 ASCII，且正点原子库文件是 GBK 编码（不能往里写 UTF-8 中文，会混编码乱码）；界面文字统一英文 + 库改动注释全英文（同 freertos.c 免疫原则）
- **光标必须无损移动**：覆盖在界面内容上的浮动层（光标/弹窗）移动时不能"填背景色擦除"，必须读回保存-写回恢复（read_area/write_area），否则路过处留下痕迹

### 明日计划
- 烧录验证：BOOT 蓝屏 2 秒 → 英文登录界面输入 1234 → 桌面；错密码红字提示、连错 3 次锁定 30 秒；光标划过文字/图标/格子不再留白块
- 桌面图标 hover 高亮 + SW 点击打开应用（Files → 阶段 4）

## 2026-08-18

### 今日进度
- [x] **实验确定 GRAM 读取语义（重点 bug 后续）**：上一版 `read_area` 用"一次 0x2E 命令 + 连续读"，实测光标恢复后出现马赛克。用 `gram_probe` 实验函数读 12 个像素（用户读屏报数：46 63488 4 64516 0 63736 0 1276 63488 63488 63520 65472），数值明显错乱——**这块 ILI9341 类面板，一次 0x2E 后连续多像素读回的是坏数据**；只有"逐像素重新设窗口 + 0x2E + 虚读 + 2 次读拼 RGB565"（`read_point` 原样）可靠。`read_area` 改为循环调用 `read_point`（约 5ms/帧，正确性优先）；`gram_probe` 已删除
- [x] **光标重写为两段式"先存后画"（修复残影残留 bug）**：旧逻辑是"移动时先画到新位置、再读旧位置屏幕内容写回"——读屏读到的是**光标自己**，写回后把残影固化。改为：画光标**前**先把外框干净背景存进内存缓冲（`cursor_save_bg`），移动时 `cursor_restore_bg` **从缓冲写回**（绝不重新读屏），顺序固定"先恢复旧位置 → 再存新位置 → 再画新光标"（覆盖重叠时恢复完新位置区域也干净了）。缓冲 72×80×2=11.25KB 静态分配，光标大小上限钳到 4
- [x] **修复数字框边光标残影（重点 bug）**：快速滑动手柄时新格高亮重绘 `fill` 只盖住格子 60×45 区域，而光标外框 54×60 上下溢出格子约 8 像素——fill 把光标"腰斩"，随后 `cursor_show` 把"截断光标"当背景存进缓冲，下次恢复固化到框边。修复：新增 `cursor_hide()`（把已存背景写回屏幕，光标消失）+ `cursor_overlap()`（矩形相交判断）；desktop 所有局部重绘（格子高亮、提示行、密码圆点、锁定倒计时）统一走 `redraw_protect_begin/end`——与光标外框相交时先隐藏、重绘完再画回，保证"存背景"永远发生在屏幕干净时；不相交则零开销直画
- [x] **修复密码圆点"蓝心残留"（擦除不彻底 bug）**：圆点从未输入"蓝色实心 7×7"改成"灰色空心框"时只画了 1px 边框，中间 5×5 蓝色残留，退格/输错清空后看着像还输着密码。修复：画空框前先 `fill` 白底整块擦除

### 设计决策
- 光标背景恢复的唯一正确姿势：**内存缓冲两段式**（先存干净背景再画，恢复用缓冲不用读屏）。"读屏恢复"在任何时序下都可能读到光标自己或截断光标，一律不用
- 浮动层（光标）与局部重绘共存的安全协议：**重绘前 hide、重绘后 show**，并配矩形相交判断——相交才保护（键盘高亮几乎每次），不相交零开销（提示行/圆点大部分时候）
- 局部重绘"擦除不彻底"自查清单：从"实心"切到"空心/框"的状态切换，必须先整块填底色再画细节（圆点、高亮格、进度条同理）
- 性能账：光标每帧成本 ≈ 恢复(写) + 保存(读 3240 点 ≈5ms) + 绘制；高亮切换帧额外一次 hide/show。事件 15ms 一帧，正确性优先，卡顿后再优化（如读窗口裁剪）

### 明日计划
- 桌面图标 hover 高亮 + SW 点击打开应用（Files → 阶段 4）
- 任务栈水位检查（uxTaskGetStackHighWaterMark，阶段 1 遗留项）

### 晚间设计讨论：时间 + 输入设备状态（明日动工蓝图）
- **时间**：内部 RTC（LSE + VBAT 电池，真实时钟）。刷新不用新任务（破坏单写者），改用 **FreeRTOS 软件定时器**（osTimerNew，1 秒周期）：回调不能阻塞，只做 `xQueueSend(EV_TICK)`；ui_task 收到后读 `HAL_RTC_GetTime` 重画状态栏 "HH:MM"。LSE 起振失败则降级软件时钟（xTaskGetTickCount）
- **输入设备状态**（出题者意图应为"设备在线状态"，非摇杆位置指示）：**K0（PE4）作为设备开关**，input_task 轮询 + 消抖产生 `EV_DEV_TOGGLE`；上电默认 OFF，OFF 时 input_task 丢弃摇杆采样和 SW（事件都不发，光标真不动）；状态显示 "JS:ON" 绿 / "JS:OFF" 红，LOGIN 和 DESKTOP 右上角同一位置（draw_js_status 复用）；OFF 时 LOGIN 提示行显示 "Joystick OFF - Press K0"
- 共享变量 `g_js_on` 不需要锁：单字节 + input_task 独占写 + ui_task 只读，CM3 单字节访问天然原子（答辩可讲）
- 演示叙事：开机 → 登录界面 JS:OFF → 按 K0 → JS:ON 光标能动 → 输密码 → 桌面

## 2026-08-19

### 今日进度
- [x] **拿到题目软件部分原文，逐条对照**：FreeRTOS✓ / 启动-密码-桌面✓ / 错误提示+锁定✓ / 桌面信息（时间+输入状态）本次完成，错误提示待 toast / 光标不出屏✓ / 应用选中打开退出返回 ✗（阶段4）/ **未连接显示状态+恢复继续操作 ✓（K0 方案正是此题点）** / 按下释放✓（EV_KEY_UP 已有）拖动滚动 ✗ / 熄屏唤醒 ✗（最高技术风险，唤醒源只能用 GPIO 按键，摇杆是 ADC 无边沿） / 应用真实运行 ✗ / 分层 bsp/dev/app（本次补 dev 层）
- [x] **dev 层抽取**：`App/Dev/joystick.c/h`——摇杆双轴采样（死区±100→灵敏度换算→步长限幅±8）、SW 边沿（DOWN/UP）、**K0 设备开关**（连续 2 次采样消抖 + 按下边沿发 EV_DEV_TOGGLE）、`g_js_on` 默认 OFF 时丢弃摇杆/SW 事件（K0 永远可扫）。input_task 瘦身为纯事件聚合（扫描→发队列）。**分层：App/Dev（设备驱动）→ input_task（事件聚合）→ ui_task/desktop（应用）**
- [x] **K0（PE4）设备开关**：用户 CubeMX 配 GPIO_Input + Pull-up（精英板 KEY0 低有效）。演示叙事：开机 → JS:OFF 红 + "Joystick OFF - Press K0" → 按 K0 → JS:ON 绿 + "Enter Password" → 光标能动。g_js_on 单字节 + 独占写/只读，无需锁（CM3 原子性）
- [x] **时间源 rtc_app.c**：内部 RTC。**坑：正点原子魔改过 F1 HAL**——RTC_InitTypeDef 只有 AsynchPrediv+OutPut（无 SynchPrediv/HourFormat，24h 固定），用 `RTC_AUTO_1_SECOND` 让库自动算分频；`__HAL_RCC_RTC_CLK_ENABLE` 宏不存在，直接 `RCC->BDCR |= RCC_BDCR_RTCEN`；BKP 寄存器从 DR1 起（无 DR0）。LSE 起振失败降级 LSI。BKP 魔数（0xA5A5）判首次上电设 12:00。hal_conf.h 手动开 HAL_RTC_MODULE_ENABLED + uvprojx 手动加 rtc.c/rtc_ex.c（CubeMX 未开 RTC 中间件，postgen 已兜底）
- [x] **1 秒软件定时器**（freertos.c USER CODE 区）：`osTimerNew` 周期定时器，回调只发 EV_TICK 进队列（定时器服务任务不能阻塞），ui_task 收事件读 RTC 重画时钟——单写者原则不破坏
- [x] **状态栏 UI**：desktop.c——LOGIN 右上角 / DESKTOP 蓝条同一位置画 `JS:ON`（绿）`JS:OFF`（红）；DESKTOP 蓝条右侧 `HH:MM`（手工拼字符串，避开 sprintf 重库）；hint 区上界 15→21 避让 JS 状态、文字按 strlen 动态居中（"Joystick OFF - Press K0" 22 字符不溢出）；EV_TICK/EV_DEV_TOGGLE 进 handle_event，重绘全部走 redraw_protect 保护
- [x] 文件系统方案定案：**FATFS on W25Q128**（精英板 SPI2：PB13/14/15 + CS PB12）——真实文件系统对抗"静态展示"要求；挂载失败自动 f_mkfs + 预置演示文件；所有 FS 操作只在 ui_task（天然单线程，无需 FatFs 线程锁）

### 设计决策
- 分层规范（题目 3.2.1 末条）：**BSP（Drivers/BSP 屏幕驱动）→ dev（App/Dev 输入设备）→ app（App/Src 任务与 UI）**，input_task 退化为事件聚合器
- 软件定时器是"每秒事件"的正解：不用新任务（破坏屏幕单写者），回调零阻塞，生产-消费模型复用
- 熄屏唤醒（题目 3.2.1 第 9/10 条）是最高技术风险：F103 STOP 模式 + EXTI 唤醒，唤醒源只能是 GPIO 按键（摇杆 ADC 无边沿），唤醒后要恢复原界面状态——排在应用框架之后，留 1.5 天

### 明日计划
- 应用框架：桌面图标 hover 高亮 + SW 打开/退出/返回桌面 + Files UI 骨架（内存模拟列表跑通 UI）
- 接真文件系统：SPI Flash（W25Q128）驱动 + FatFs 移植 → f_readdir 真目录 → txt 查看器

### 晚间：应用框架落地（K1 返回键 + 注册表 + hover + 打开/返回）
- [x] **K1（PE3）返回键**：joystick.c 复用 K0 消抖模式（连续 2 次一致 + 按下边沿）产生 EV_BACK；与 K0 一样**永远可扫**（板载按键，不依赖摇杆设备）。语义 = **逐级回退**：应用内 → 返回桌面；桌面 → 锁屏回登录页（重置连续错误计数）；登录页 → 清空已输密码（"取消输入"）
- [x] **应用注册表**：`app_t`（name/color/open/handle）+ `g_apps[]` 表（app.c）+ 公共标题栏 `app_draw_title`（蓝条 + 应用名 + "K1:Back" 提示）。桌面图标绘制 / hit 命中 / hover / SW 打开**全部遍历注册表**，框架不认识任何具体应用（结构体+函数指针模拟多态，答辩点）
- [x] **桌面态内嵌应用态**：`s_cur_app` 指针非空 = 应用前台态，所有事件（含 EV_TICK/EV_DEV_TOGGLE）**全量转发**给应用 handle()——Monitor 每秒刷新靠转发后的 EV_TICK，公共层不再截留
- [x] **图标 hover 白框高亮**：hit_icon 遍历注册表命中（光标中心），变化时并集矩形（含 2px 外框余量）走 redraw_protect 协议重绘旧+新图标——第三次复用光标保护协议
- [x] **打开/返回**：SW 命中图标 → `launch_app`（先 cursor_hide 保证光标状态干净，再让应用自己全屏绘制）；K1 应用态 → enter_desktop 重绘桌面
- [x] **System Monitor（第一个真实应用）**：`app_monitor.c`——uxTaskGetSystemState 枚举任务（名字 + 栈剩余水位 usStackHighWaterMark×4=字节）+ xPortGetFreeHeapSize；EV_TICK 每秒刷新；纯显示不占用光标。**验证了完整链路：打开 → 运行 → K1 返回**
- [x] 图标布局 2×2 → **3×2**（6 个应用）：68×60 色块 + 名字在块下
- [x] 编译 0 Error / 0 Warning，Code=24372

### 修复：应用内按 K1 退不出来（bug）
- **现象**：进入 Monitor 后按 K1 无反应
- **排查**：按 K1 应走 UI_DESKTOP 应用态分支 → 检查发现实现把"应用态所有事件全转发给应用"，EV_BACK 也被转给 Monitor，而 Monitor 的 handle 只认 EV_TICK——导航事件被应用吞掉
- **根因**：实现与设计不一致（设计时已定 EV_BACK 为系统级事件由框架接管，写代码时却写了全转发）
- **解决**：应用态分支先拦截 EV_BACK（框架直接 enter_desktop 返回桌面），其余事件才转发给应用 handle
- **教训**：事件路由要区分"系统级事件"（导航/电源类，框架独占）与"应用级事件"（转发）；写实现时要对着设计逐条核对，别凭记忆

### 修复：应用界面残留桌面图标方块残影（bug）
- **现象**：进入 Monitor 后第一次滑动摇杆，在原光标位置留下应用图标颜色的方块
- **排查**：方块 = 图标色块，来自**打开应用前存的桌面背景**。launch_app 时 cursor_hide 把光标藏起（背景缓冲仍是桌面内容），应用全屏绘制后，第一次摇杆移动触发 cursor_move——**它不检查光标是否可见**，直接 cursor_restore_bg 把缓冲里过期的桌面背景写回应用界面
- **解决**：cursor_move 开头判断 `!g_visible` 直接返回（不恢复、不重绘、不更新坐标）；应用需要光标时自己 cursor_init+show 定位
- **教训**：光标模块的所有动作（move/hide/show/overlap）都应以 g_visible 为准绳；"隐藏"后的任何重绘路径都要能自洽，别让隐藏状态残留副作用

### 调整：应用内统一显示光标（用户反馈 + 防御）
- **反馈**：进入应用后摇杆移动看不到光标；返回桌面后光标停在第一个图标（Files 蓝色块）上
- **结论**：应用内无光标是 Monitor"纯显示"设计造成的预期差——**统一改为所有应用打开后都显示光标**（stub/Monitor 各自 cursor_init 到内容区下方空白 + cursor_show，屏内安全位外框不越界）
- **顺带防御**：draw_content 的 `TaskStatus_t ts[8]`（约 320B）从栈上改到静态区——ui_task 栈 1KB，叠上 atk 绘制调用链余量不大，静态化彻底消除隐患
- 返回桌面光标定位第一个图标中心 = "默认选中第一个应用"设计（enter_desktop 统一初始化），摇杆持续推动即可移出

### 晚间：System Monitor 补全（对照题目 8 项要求）
- [x] **系统运行时间 Up**：xTaskGetTickCount()（1kHz）÷ 1000 → 手工拼 "H:MM:SS"（避开 sprintf 重库）；uint32 约 49.7 天回绕，演示期无碍
- [x] **输入事件计数 Ev / 丢弃计数 Dr**：sys_stats 统计模块（App/Src/sys_stats.c）——input_task 每次产生事件 +1；xQueueSend 返回 pdFAIL（队列满=背压）时丢弃 +1。**丢弃数能真实反映队列背压**（锁屏 30 秒期间事件堆积到 8 就会丢），答辩亮点
- [x] **错误计数 Er**：定义 = 系统级异常累计，首个埋点 = RTC LSE 起振失败降级 LSI（rtc_app）；预留文件系统挂载失败等
- [x] **任务运行状态 St**：uxTaskGetSystemState 的 eCurrentState → 状态字母（沿用 vTaskList 惯例 X=运行 R=就绪 B=阻塞 S=挂起 D=删除），X 用蓝色突出当前执行任务
- [x] 布局：Up / Heap / Ev-Dr-Er / Task-St-Stack(B) 列头 + 任务行（5 任务，行高 20）
- [x] **并发**：volatile uint32_t 统计变量——input_task 独占写、ui_task 只读，CM3 字对齐访问硬件原子，无需锁（与 g_js_on 同原理）
- [x] 编译 0 Error / 0 Warning
- 题目对照：运行时间✓ 系统状态✓（任务状态列+运行中高亮） 事件计数✓ 丢弃计数✓ 错误计数✓ 任务运行状态✓ 栈水位✓（原有） 剩余内存✓（原有 Heap）——8 项全齐

### 设计决策
- **注册表 = 应用"名片"表**：桌面只认"表里的第几项"，加应用 = 写一个文件 + 表里加一行，框架零改动（C 语言多态，答辩可讲）
- **launch_app 协议**：进入应用前强制 cursor_hide（光标状态归零），应用自行决定是否显示光标（Monitor 不显示、Files/Paint 显示）——避免"半截光标存进背景缓冲"类残影问题
- 图标 hover 高亮画"外框 2px 白框"：先整块擦白（盖掉旧白框）再画色块再画框——擦除不彻底自查清单的第 3 次应用
- 应用不自己开定时器，**复用系统 EV_TICK**（1 秒心跳）——单写者原则保持，应用代码也简单

### 明日计划
- Files：W25Q128 SPI 驱动（BSP 层）+ FatFs 移植（f_mount 失败 f_mkfs + 预置演示文件）→ 列表 UI + txt 查看器（注册表替换 stub 行）
- 剩余应用逐个替换 stub：Monitor 已完成，Files → Paint → Settings → Logs → Music

### 8-18 晚：Files 应用落地（W25Q128 + FATFS R0.16）
- **硬件准备**：SPI2 = PB13/14/15（Mode0，18MHz=APB1 36MHz÷2），CS = PB12 软件 GPIO（W25Q_CS，输出上拉、初始 High）——CubeMX 配置后 postgen 恢复工程，编译基线确认
- **驱动层 w25q128.c**：JEDEC ID（0x9F → EF 40 18）校验；读 0x03 连续不限页；写 0x02 按 256B 页自动切页（页编程不能跨页边界，芯片硬约束）；擦 0x20 只到 4K 粒度；WIP 轮询 + 超时兜底
- **移植层 diskio.c**：512B 扇区 × 32768 扇区（16MB）；**写路径 = 读-改-擦-写 + 4K 块缓存**——FATFS 按扇区写而 Flash 按 4K 擦，若每扇区都擦会拖垮（~100ms/次）；缓存命中时只改内存，换块/CTRL_SYNC 才擦写整块，擦除次数降到理论下限
- **Files 应用**：目录列表（文件名+大小，蓝条高亮选中行，光标推到边缘自动滚动）；SW 打开文件 → 文本视图（读 800B 压成 20×40 网格，丢弃 \r\n）；再按 SW 关闭；K1 退出（框架接管）
- **首次上电自动建卷**：f_mount 返回 FR_NO_FILESYSTEM → f_mkfs（FM_FAT，work 缓冲 4KB 静态区）→ 重新挂载 → 预置 README.TXT/DATA.TXT（内容带 RTC 时间戳）
- **get_fattime**：文件时间戳接内部 RTC——rtc_app 新增 rtc_app_get_datetime（GetTime 后必须 GetDate 解锁 shadow 寄存器）
- **栈扩容**：uiTask 1KB → 2KB（FATFS 调用链跑在 ui_task 栈上；osThreadNew 在 USER CODE 块内，CubeMX 重新生成不丢）
- 编译 0 Error / 0 Warning；FATFS 配置要点：FF_USE_MKFS=1、FF_CODE_PAGE=437、FF_USE_LFN=0（8.3 短名）、FF_FS_NORTC=0（接 RTC）

#### Bug：postgen IncludePath 加了等于没加（误判"已有"跳过）
- **现象**：uvprojx 编译报 `cannot open source input file "ff.h"`；postgen 却提示"包含路径已含 ../App/FatFs，跳过"
- **复现**：运行 postgen → UV4 编译 → ff.h 找不到
- **根因**：Contains("../App/FatFs") 检查的是整个工程文本——**同一轮运行里 2a 步骤刚插入了 `<FilePath>../App/FatFs/ff.c</FilePath>`**，子串匹配命中 FilePath 条目，2b 的 IncludePath 检查误判"已存在"直接跳过，实际 IncludePath 从未写入
- **解决**：检查改为精确匹配 `"../App/FatFs;"`（带分号，只可能出现在 IncludePath 条目）；顺带统一 App/Inc、App/Dev 检查口径
- **教训**：脚本里的"已存在"检查要用能区分上下文的精确模式（文件条目 vs 路径列表），子串太宽会自欺
- **顺带观察**：UV4 -b 每次会重写 uvprojx（时间戳变化），但内容完整往返——postgen 幂等修复仍是安全网


#### Bug：刷入新固件后 LED 闪烁但屏幕全黑（uiTask 静默丢失）
- **现象**：FATFS 版本固件刷入后，默认任务 LED 正常闪烁，但 LCD 全黑、无背光、无任何绘制
- **复现**：100% 复现——每次上电都黑屏；退回 Monitor 旧版固件（b3a9db1）则正常显示
- **排查路径**：
  1. 黑屏特征 = `atk_md0280_init()` 的芯片 ID 校验失败提前 return（背光在 ID 通过后才打开，ID 失败屏幕物理全黑）→ 一度怀疑 FSMC/时序/SPI 引脚冲突
  2. 逐一 diff 新旧提交：LCD/FSMC BSP 零改动、主时钟未动、SPI2 引脚(PB13-15/PB12)与 FSMC(PD/PE)、背光(PB0)零冲突 → 排除硬件初始化代码
  3. 疑点收敛到唯一运行时差异：**uiTask 栈 1KB → 2KB（FATFS 调用链需要）**
  4. 查 `FreeRTOSConfig.h`：`configTOTAL_HEAP_SIZE = 3072`（3KB）——旧构建各任务+队列已占约 2.5KB，+1KB 后 **uiTask 的 osThreadNew 堆分配失败返回 NULL**，任务根本没被创建
- **根因**：3072 字节的堆装不下"3 个应用任务 + timer/idle + 队列 + uiTask 2KB 栈"。`osThreadNew` 分配失败是**静默的**（返回 NULL，无断言、无钩子），defaultTask 先创建成功所以 LED 照常闪——"主循环活着但界面任务死了"的黑屏
- **解决**：`configTOTAL_HEAP_SIZE` 3072 → **8192**（编译后 RAM 总占用 35.7KB/64KB，余量充足）；开启 `configUSE_MALLOC_FAILED_HOOK` + `vApplicationMallocFailedHook()`（堆耗尽 → for(;;) 冻结，LED 停闪 = 分配失败信号，不再静默）；postgen.ps1 新增第 4 节把这两处改动固化为"CubeMX 重新生成后自动恢复"
- **教训**：FreeRTOS 任务创建失败不会叫——**任务没起来要第一个怀疑堆**。栈分配前先算总账（TCB+栈 ≈ 600B/任务，timer 任务 1KB 栈是隐藏大户）；`configTOTAL_HEAP_SIZE` 3KB 属于"刚好能跑就没人动"的定时炸弹，任何 +1KB 都会踩爆
