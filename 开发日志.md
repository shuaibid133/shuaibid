# 开发日志（KazepOS）

> 考核要求：每天记录做了什么、踩了什么坑、怎么修的。配合 Git 提交看更完整（每条对应一个存档）。
> 格式：每个 bug 三句话——**问题**（现象）/ **原因** / **修法**。

## 2026-08-15 工程搭起来

- CubeMX 生成 FreeRTOS 工程（F103ZET6 / 72MHz），Keil 编译下载跑通，LED 在默认任务里闪（RTOS 能跑）
- 移植正点原子 2.8 寸屏例程（FSMC 接口），白底红字 "Hello RTOS!"
- 输入设备定为**摇杆**，**砍掉触摸**（`USING_TOUCH=0`，输入只走摇杆）
- 光标大小/灵敏度做成参数（`sys_config_t`），给后面的 Settings 用
- 建 Git 仓库

## 2026-08-17 输入系统 + 密码登录 + 桌面框架

做了：
- 光标调成 Windows 经典箭头样式（16×18 位图 + 白色描边），尺寸可参数放大
- `input_task` 每 15ms 扫一次摇杆（死区、灵敏度换算、按键边沿检测），事件发进队列；`ui_task` 收事件动光标——**生产者/消费者解耦**
- 桌面框架 v1：开机画面 2 秒 → 密码登录（4 位，连错 3 次锁 30 秒）→ 桌面

踩坑：
1. **postgen 脚本等长替换出错**：替换串比原串长 3 字节，把下一行代码吞了，HAL 的 TIM 宏丢了一个 → 编译报错。改成纯文本级替换 + 脏行自愈
2. **光标路过文字/图标留白块**：原来"擦光标"用白矩形盖，等于把背景擦坏了。改成先读屏保存背景、移动后再原样写回（**无损移动**）
3. **所有中文不显示**：屏库 `show_string` 只支持 ASCII，遇到中文直接不画。决策：**界面文案全部用英文**

重要决定：
- **单写者原则**：屏幕只许 `ui_task` 画，别的任务禁止碰屏，防并发抢总线画面撕裂
- 光标是浮动层：移动 = 先存背景 → 动 → 恢复背景，禁止"填背景色擦除"

## 2026-08-18 光标残影攻坚

踩坑：
1. **读屏恢复背景出现马赛克**：这块 ILI9341 类面板"一次命令连续读多个点"读回的是坏数据，只能逐点设窗口读——慢（每帧约 5ms）但正确
2. **残影被固化**：先画新光标再读旧位置，读到的是光标自己 → 改成两段式：**画之前**先把下一步要到的地方的背景存进内存缓冲，移动时从缓冲写回，绝不重新读屏
3. **键盘高亮把光标"腰斩"留残影**：局部重绘和光标打架 → 定协议：重绘区跟光标外框相交就先藏光标、画完再画回（`redraw_protect`，不相交则零开销）
4. **密码圆点有蓝心残留**：从实心改成空心框时没先擦干净 → 状态切换必须先整块填底色再画细节

晚上定下两个设计：
- 时间源 = 内部 RTC + **FreeRTOS 软件定时器**每秒发一条"1 秒心跳"事件，ui_task 收到就去读 RTC 刷时钟——不加新任务、不破坏单写者
- 输入设备状态：K0 按键当"设备开关"，默认 OFF（摇杆输入全丢），按 K0 变 ON；屏幕右上角显示 JS:ON（绿）/ JS:OFF（红）

## 2026-08-19 ~ 08-21 对照题目 + 应用框架 + Files 应用

- 拿到题目原文逐条对照，缺口排优先级；"熄屏唤醒"记为最高风险项（F103 STOP 模式唤醒源只能是按键，摇杆是 ADC 没有边沿）
- 分层定稿：**BSP（屏幕）→ dev（摇杆）→ app（任务+UI）**；摇杆逻辑抽到 `App/Dev/joystick.c`，input_task 瘦成"扫到就发队列"
- K0 设备开关落地（连续 2 次采样一致防抖）；`g_js_on` 单字节、input_task 独占写 / ui_task 只读，无需锁
- RTC 落地。坑：正点原子魔改过 HAL（RTC 结构体缺字段），用库的 `RTC_AUTO_1_SECOND` 自动算分频；LSE 起振失败降级 LSI；BKP 魔数判首次上电
- 1 秒软件定时器回调只做一件事：发 EV_TICK 进队列（回调不能阻塞）
- 状态栏：登录/桌面同一位置显示 JS 状态 + 时间 HH:MM
- 文件系统定案：**FATFS on W25Q128**（SPI2）；挂载失败自动格式化 + 预置演示文件；所有文件操作只在 ui_task 里跑（天然单线程，免锁）

应用框架（晚间）：
- K1 返回键，语义 = **逐级回退**：应用内 → 桌面；桌面 → 锁屏；登录页 → 清空已输密码
- **应用注册表** `g_apps[]`：桌面只认"表里的第几项"，加应用 = 加一行表，框架零改动
- 打开应用后所有事件转发给它的 handle()，应用自己画自己；桌面框架不管具体应用
- System Monitor 做成第一个真实应用：枚举任务（名字/状态/栈剩余水位）+ 剩余堆内存，每秒刷新——验证了"打开→运行→K1 返回"全链路

踩坑：
1. **应用内按 K1 退不出来**：EV_BACK 也被"全转发"给了应用，被吞掉。修：**系统级事件框架先拦截**，应用级事件才转发
2. **进应用后留下桌面图标色块**：进应用前光标已藏，但它内存里的背景还是桌面的；应用里第一次动光标，把过期背景写回屏幕。修：`cursor_move` 开头判断"光标没显示就不动"
3. 用户反馈应用内看不见光标：统一为**所有应用打开后都显示光标**（Monitor 原先纯显示，也在空白处给一个）
4. 顺手防御：Monitor 的大结构体从任务栈挪到静态区（栈余量紧，叠上绘制调用链怕爆）、、、、、、、、、、、、、、、、、、、

Files 应用（W25Q128 + FATFS，5 个坑连续踩）：
1. **HAL SPI2 读 Flash 冻死全系统**（LED 停闪、连故障报告都不出）：阻塞传输不返回。修：弃用 SPI2 外设，**改 GPIO 位带驱动**（自己模拟 SPI 时序）——从此稳定
2. **读芯片 ID 恒 0**：寄存器地址写错——把只写的 BSRR 当 IDR 读。改正确地址后 ID 出来了
3. **自检误报引脚故障**：写寄存器后立刻读是旧值，加延时再读就正常
4. **ID 不是原厂的**：0x00522118 vs 原厂 0xEF4018——板上是**克隆片**，功能正常。修：ID 检查放宽（有响应 + 容量字节对即可），再加"擦→写→读→比对"真实自检
5. **断电后文件消失**：4K 写缓存只在换块时才落盘，最后一块不足 4K 永远不会触发 → 格式化/建文件结尾强制落盘（CTRL_SYNC）
- 文件时间戳接内部 RTC（读时间后必须再读日期，解锁 shadow 寄存器）
- uiTask 栈 1KB→2KB（FATFS 调用链吃栈）。踩坑：**FreeRTOS 堆只有 3KB，+1KB 栈后任务创建静默失败（返回 NULL 不报错）→ 屏幕全黑但 LED 在闪**。修：堆 3KB→8KB + 开堆耗尽钩子（以后再失败会直接停住，不再静默）
- postgen 又坑一次：判断"包含路径已存在"用了太宽的子串，把刚插入的 .c 文件路径也匹配上 → 误判跳过。改精确匹配

Files 补齐题目功能（文件表 5 字段 + 新建/删除/重名检测）：
- 内存文件表：名字/类型/状态/大小/簇；工具栏 New/Del；新建自动编号 FILE001.. 并查重重名 +1
- 删除二次确认："Delete? SW=Y Del=N"
- 坑：**删除永远进不了确认态**——选中行跟光标位置绑定，光标一移到按钮上"选中"就没了。修：加"最后选中"记录，删除看它
- 坑：K1 想用来取消删除，但 K1=返回是框架独占的 → 取消改为"再点一次 Del"
- 坑：退出文件查看后界面残留旧文字——查看是全屏画的，退出只重绘了部分区域 → 退出先整屏擦白再重画

## 2026-08-22 个性化 + 演示文件升级 + 校时

- 系统改名 **KazepOS**（开机大字、状态栏、占位页、文件模板全部替换）
- 演示文件 README/DATA 内容升级贴齐项目背景；旧卷自动升级（只重建演示文件、保留用户新建的）
- 坑：**新建文件时间戳是 2000 年**——RTC 只设过时分秒，日期寄存器从未设。修：首次上电用编译器宏（`__DATE__`/`__TIME__`）拿到**编译时刻**写入日期+时间
- 开机校时流程：BKP 魔数判"是否校过时"——没校过（首次上电/断电过）→ 自动进 Set Clock 界面输 8 位 MM-DD HH:MM → 写 RTC + 写魔数 → 登录；K1 可跳过
  - 设计修正说明（8-29 实测）：当时按"精英板无备份电池"假设做的每次开机强制校时；**实测发现板子带 CR1220 纽扣电池**，断电后 RTC 魔数和时间都保持——所以正常开机直接进登录，校时界面只在首次上电（或换电池）后出现

## 2026-08-23 ~ 08-24 Settings + 背光 PWM + Paint 保存攻坚

Settings 应用：
- 6 行设置：灵敏度/光标大小/亮度/音量/熄屏时间/校时入口；每行右侧 [-] [+] 按钮，SW 逐格调
- 即改即生效（灵敏度实时读配置；亮度直接写 PWM）；调节后 2 秒没操作自动存 Flash
- 配置持久化 = **裸写 W25Q 专用扇区**（不用 FATFS 存：登录界面就要用，等不起挂载）：magic "KZ" + 字节和校验 + 数据；校验不过 → 回默认值 + 记日志（脏数据不带病启动）
- 背光从 GPIO 开关改 **PWM**（20kHz 无闪烁），亮度可调
- 熄屏：空闲秒数到设定值就灭背光（内容保留），任意输入唤醒

踩坑：
1. **烧 PWM 版后黑屏**：TIM 配置结构体没清零，垃圾值写进寄存器 → PWM 没输出、背光引脚浮空。修：清零 + 所有 HAL 调用查返回值 + **失败回退 GPIO 全亮**（屏幕永远可见）
2. 链接报 undefined：新加的 .c 没进 Keil 工程。约定：**新文件一律 Keil 图形界面添加**，不再手改工程文件
3. 用户反馈"隔一会黑屏"——其实是熄屏功能正常触发，不是故障

Paint 保存攻坚（克隆片 Flash 时序，最惨烈的一天）：
1. **保存 10-20s 且存出的图全白**：克隆片数据输出建立时间远大于原厂，位带采样点太早 → 状态寄存器读错 → 以为擦完了其实还在忙 → 页编程被芯片忽略 → 全 0xFF。修：实测三个采样点（60 NOP 正确 / 8 错 / 30 仍错），固定用 60 NOP（0.83us）
2. **免擦优化**：写前先看目标块是否全 0xFF（干净）——干净就跳过擦除直接写。保存从 40s 级降到 4s 级
3. **格式化后保存还是慢**：f_mkfs 只写卷头，数据区残留旧数据 → FMT 改成**先全片擦除**（40-60s 一次付清）再建卷
4. **保存后所有文件消失**（最严重）：根目录 16 项写满 → 自检临时文件建不了 → 返回 255 → 旧代码一看 255 就无条件全盘格式化。修：老卷自动升级 512 项根目录；错误码细分（目录满 ≠ 卷坏，绝不格式化）；只有卷真读不了才 FMT
5. **第二次保存文件又全消失**：跳过自检时一个标志残留 255 被误判失败 → 自检通过就直接置 0
- 保存后加"读回验证"兜底（统计非白像素）+ 目录项自检；诊断体系：保存前显示目录健康 P、失败显示 E1~E8 精确到哪步错、耗时后显示擦除块数

## 2026-08-25 Music + Logs

Music（蜂鸣器播放器）：
- 底部 Prev/Next/Mode 按钮 + 顺序循环/单曲循环/随机三种模式，播完自动续
- 随机用 LCG 线性同余伪随机，避开当前曲

踩坑（曲谱数据连环）：
1. **切歌后屏幕新歌、耳朵旧歌**：播放任务分片延时醒来只查暂停、没查"歌被切了"。修：切歌立即中断剩余延时，响应 ≤50ms
2. **三种模式表现乱**：一个变量兼职"列表高亮行"和"播放行" → 拆成两个变量，续播永远基于播放行
3. **每首结尾串进下一首旋律**：音符数算错——每个音符占 2 个 uint16，`sizeof/2` = 元素个数 = 音符数×2 → 越界读到下一首歌。修：`sizeof/4`，6 首逐一核验
4. **整体低半音 + C 音消失**：曲谱约定"0=C4"、频率表约定"0=休止"，两套差 1 → 统一约定，曲谱全部 +1
5. **底部按钮把光标盖没**：按钮区重绘漏了"先藏光标"协议 → 补上

Logs：
- 日志 = RAM 环形缓冲 32 条（级别/消息/参数/时分秒），重启即清（演示期不写 Flash——擦一次 1.5~2s 还烧寿命，权衡写死在注释里）
- 界面倒序列表（最新在顶），时间|级别|消息|参数，SW 清空，每秒拉快照刷新
- 25 类消息铺满题目 7 个验收点埋点（登录失败/文件操作/设置修改/设备断开恢复/熄屏唤醒/错误事件/清空）
- 坑：删文件、画几笔后进 Logs 没有新条目——初版只在失败操作埋点，成功操作没记 → 补齐成功类埋点（File created/deleted）
- 架构改动：设备开关埋点在 input_task（第二个写者）→ `sys_log_add` 加**关中断临界区**保护环形缓冲

## 2026-08-26 ~ 09-01 进阶验收点 ①~⑥

### ④ 运行负载分析
- 统计量分两侧采（遵守单写者）：生产侧 = 事件总数/每秒吞吐/丢弃数；消费侧 = 排队延迟/队列峰值积压/合并数
- **实测猛推摇杆延迟峰值 ≈ 30ms**（消费跟不上生产）→ 直接引出 ① 的优化
- Monitor 显示；丢弃数 = 队列满的"堵车证据"（锁屏 30 秒期间事件堆满就会丢）

### ③ 存储一致性保护
- 配置扇区：magic "KZ" + 字节和校验；校验不过 → 回默认值 + 记 `LOG_CFG_CORRUPT`（留证据，不静默）
- 画图文件：保存/加载带校验，损坏 → `LOG_IMG_CORRUPT` 拒绝预览
- 贯穿原则：**任何持久化数据都要能自证清白**

### ① 输入事件优化
- 消费侧合并（ui_task）：取出事件后把队列里紧随的移动事件捞出来，dx/dy 累加成一条——光标只动一次；捞到非移动事件插回队首（全系统单消费者，无竞态）
- 生产侧优先级（input_task）：队列满时**按键不可丢**，挤掉队首移动事件让位（移动是增量可合并，少一条只少动一点）
- 效果：Dly-Max 30ms 回落，K1 响应正常
- 坑：**双光标**（用户实测）——界面切换时 fill 全屏盖掉光标但内部状态没同步。修：三个界面切换函数开头统一先藏光标

### ② 任务异常检测（两层防护）
- **硬件层 IWDG**：2s 超时（LSI 40k/64×1250），使能后关不掉；喂狗者 = input_task（15ms 一次、从不长阻塞）
- **软件层心跳哨兵**：每个任务在自己循环里打卡，input_task 每 15ms 代查；阈值按任务放宽（input 1s / music 3s / ui 2s）；没卡过的跳过防开机误报；超时 → `Task hung`（去抖只报一次），恢复 → `Task recovered`
- 分工一句话：**喂狗链不断，IWDG 就是瞎子**——IWDG 只管喂狗者卡死，别人卡死靠哨兵
- LED 诊断体系（ui_task 单写者）：每秒闪 = 正常 / 停闪 = ui_task 卡死 / 2s 一亮一灭 = IWDG 复位循环
- 演示 A：`WDG_DEMO_INPUT_HANG` = 1 → 开机 10s 后死循环 → IWDG 复位重启（正常→卡死→自动重启）
- 演示 B：`WDG_DEMO_UI_HANG` = 1 → 开机 10s 起假卡死 10s（屏幕定格）→ 哨兵 2s 内报警 → 窗口结束自动恢复

踩坑：
1. **烧录后 LED 常亮不启动**：input_task 优先级最高最先跑，看门狗还没初始化就喂狗 → 喂到 NULL 句柄 HardFault。修：未初始化不喂
2. **演示 A 一开机就复位循环**：死循环放在循环第一行，屏幕永远来不及亮。修：卡死延迟到开机 10s 后
3. **演示 B 没效果**：music 任务是打开应用才创建的，开机时不存在 → 卡死窗口从任务创建时刻算；后来演示对象直接换 ui_task（屏幕定格最直观）
4. **演示 B 卡死不恢复**：写成永久死循环 `while(1)` 了——"永久卡死"靠 IWDG（演示 A）和"暂时卡死"要自己恢复（演示 B）是两种写法。修：`vTaskDelay(100)+continue`，让出 CPU、每 100ms 回来查窗口
5. **卡死时 LED 还在闪**：CubeMX 模板默认任务也在闪 LED，抢了诊断灯 → 模板任务清空，LED 归 ui_task 一个写者

### ⑥ 伪 OTA
- "伪"在哪：升级包是代码里 const 数组（模拟服务器）；机制全真
- 更新区 2 扇区：包头（"KZOTA!"+版本+CRC32+长度+状态）+ 镜像；状态机 0x5A 待应用 → 0xA5 已应用 → 重启生效
- **断电安全**：先写镜像、**包头最后写**——断电时包头还是 0xFF = "无更新"，半截包永远不会被当成新版本
- 下载完**从 Flash 读回**重算 CRC32（不信任内存，校验"真写进去了"）
- 开机 boot_check：0xA5 → 版本生效；0x5A（应用步骤断电）→ CRC 再验，好补应用、坏回退；包烂 → 回退。放在哨兵首次打卡前执行（擦写不误报）
- 演示：Check Update 全自动（擦除→下载进度→校验→应用→自动重启→开机画面 V2.0）；Inject Bad 注入坏包 → 回退 + ERR 日志
- 设计修正（用户提出"V1 V2 没区别"）：版本不能记在更新区（Paint 格式化一擦就悄悄丢）→ **版本写进配置扇区，与更新包解耦**，只有恢复出厂才回 V1；开机画面显示版本号
- 心跳豁免：擦扇区 1.5~2s 超哨兵阈值会误报 → `sys_watch_set_interval` 临时拉大窗口再恢复（今天已下沉到驱动层，见下）

## 2026-09-02 ~ 09-03 界面美化 + 心跳误报修复

- 桌面改版：扁平化应用图标（彩色圆角块 + 白色几何符号，纯代码绘制）、淡蓝科技风背景、登录界面深色底、应用标题栏改品牌色（亮底自动黑字）
- 开机画面：logo + 进度条动画（与 200ms 时钟节拍对齐，纯动画不绑真实初始化）
- 应用内选中高亮统一成品牌色（Music 品红 / Settings 黄底黑字 / Paint 绿 / OTA 红，与标题栏配对）
- 修复 **Task hung 误报**：Files 删除/新建、Paint 保存会触发 Flash 擦除（4K 擦 1.5~2s、全片擦 40-60s）且全程忙等——超过哨兵 2s 窗口被如实误报成卡死（只有 OTA 当时做了豁免）。修：豁免下沉到 **w25q128 驱动层**（擦除期间窗口拉到 20s / 2 分钟，擦完恢复 2s），FATFS 回写、配置保存、OTA 全路径统一覆盖
- 新增《微型操作系统一些问题.md》（名词解释 + 进阶点整理，答辩复习用）

## 当前状态（2026-09-03）

- 7 个应用 + 进阶 ①~⑥ 全部完成并验证；演示宏三处默认 0（答辩改 1 编译烧录，演示完改回 0）
- 剩余：mock 答辩稿、发代码到 185866315@qq.com（验收前 1-2 天，用户自理）
