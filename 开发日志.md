# 开发日志

> 考核要求：每天记录 bug、排查过程、解决方案。Git 提交配合日志一起做。

## 2026-08-15

### 今日进度
- [x] CubeMX 生成 FreeRTOS 工程（F103ZET6 / 72MHz / FREERTOS 中间件），Keil 编译下载跑通
- [x] LED 在默认任务中闪烁（RTOS 调度、vTaskDelay 验证通过）
- [x] 正点原子 ATK-MD0280 例程（FSMC 2.8 寸屏）下载验证，屏幕硬件正常（红字三行 + 3D 旋转立方体 + 触摸拖动）
- [x] LCD 驱动移植进工程：`Drivers/BSP/ATK_MD0280/`（atk_md0280.c/h、fsmc、font），触摸关闭（`USING_TOUCH=0`），工程编译通过
- [x] 屏幕点亮：白底 + 红色 "Hello RTOS!"，LED 慢闪 = 初始化成功
- [x] 建 Git 仓库，初始提交

### 遇到的问题
1. **下载 MD0280 例程后屏幕显示"英文 + 准心图标"，不是例程应有的旋转立方体**
2. **移植的驱动中文注释全部乱码**（正点原子例程是 GBK 编码，工程是 UTF-8；且编辑过程中 GBK 字节被按 UTF-8 重写，注释永久损坏）
3. **编译时 FSMC 初始化缺少依赖**：`HAL_SRAM_Init` 需要 `stm32f1xx_hal_sram.c` 和 `stm32f1xx_ll_fsmc.c`，但 CubeMX 生成的工程没有把它们编进去

### 排查过程
- 问题 1：对照 readme 和 demo.c 确认例程应有的现象（红色三行字 + 蓝色旋转立方体），屏幕显示内容不是本例程的输出 → 判断 Flash 里还是出厂综合例程，下载没生效
- 问题 2：用 `file` 命令检查文件编码（ISO-8859 = GBK 字节），grep U+FFFD 确认损坏范围，从原例程重新拷贝恢复
- 问题 3：grep `.uvprojx` 确认 hal_sram/ll_fsmc 引用数为 0

### 解决方案
- 问题 1：检查下载工具，**BOOT0 跳线拨回 0 后断电重启**（串口 ISP 下载的经典坑）
- 问题 2：从原例程重拷文件 → `iconv GB18030 → UTF-8 带 BOM`（带 BOM 的 UTF-8 在 Keil 和 VS Code 都能正确显示中文）→ 重新打上功能修改
- 问题 3：uvprojx 中把 `stm32f1xx_hal_sram.c`、`stm32f1xx_ll_fsmc.c` 加入 HAL 组，include path 加 `../Drivers`；`stm32f1xx_hal_conf.h` 启用 `HAL_SRAM_MODULE_ENABLED`（注意：CubeMX 重新生成会还原，需重开或改在 CubeMX 里配 FSMC）

### 设计决策
- 输入设备用摇杆（双轴 ADC + SW 按键），**触摸功能砍掉**（`ATK_MD0280_USING_TOUCH = 0`），输入只走摇杆
- 光标**大小和灵敏度参数化**：`sys_config_t` 结构体（cursor_size / cursor_sens 默认值 3 / 5），灵敏度用在摇杆→步长换算处，大小用在光标绘制函数——为阶段 4 设置应用预留，实现"改结构体即生效"；跨任务共享加锁推迟到设置任务出现时

### 明日计划
- 摇杆接线：VRx/VRy → 2 个 ADC 通道（接 3.3V），SW → GPIO 内部上拉；杜邦线固定牢（考核要求不得手扶线材）
- 输入任务 + 事件队列：15ms 轮询采样、死区处理、SW 去抖、归一化事件（MOVE/PRESS/RELEASE）
- UI 任务：消费事件队列 → 光标移动（边界钳制）→ 绘制（大小参数化）
- 验证任务栈水位（`uxTaskGetStackHighWaterMark`）/ 剩余内存（`xPortGetFreeHeapSize`）
