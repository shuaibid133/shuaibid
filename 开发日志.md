# 开发日志

> 考核要求：每天记录 bug、排查过程、解决方案。Git 提交配合日志一起做。

## 2026-08-15

### 今日进度
- [x] CubeMX 生成 FreeRTOS 工程（F103ZET6 / 72MHz / FREERTOS 中间件），Keil 编译下载跑通
- [x] LED 在默认任务中闪烁（RTOS 调度、vTaskDelay 验证通过）
- [x] 正点原子 ATK-MD0280 例程（FSMC 2.8 寸屏）下载验证，屏幕硬件正常（红字三行 + 3D 旋转立方体 + 触摸拖动）
- [x] LCD 驱动移植进工程：`Drivers/BSP/ATK_MD0280/`（atk_md0280.c/h、fsmc、font），触摸关闭（`USING_TOUCH=0`），工程编译通过
- [x] 屏幕点亮：白底 + 红色 "Hello RTOS!"，LED 慢闪 = 初始化成功
- [x] 建 Git 仓库，初始提交

### 设计决策
- 输入设备用摇杆（双轴 ADC + SW 按键），**触摸功能砍掉**（`ATK_MD0280_USING_TOUCH = 0`），输入只走摇杆
- 光标**大小和灵敏度参数化**：`sys_config_t` 结构体（cursor_size / cursor_sens 默认值 3 / 5），灵敏度用在摇杆→步长换算处，大小用在光标绘制函数——为阶段 4 设置应用预留，实现"改结构体即生效"；跨任务共享加锁推迟到设置任务出现时

## 2026-08-17

### 今日进度
- [x] 光标形状 8 轮迭代定稿：16×18 位图 + 8 邻域膨胀白色描边（Windows 经典箭头风格：38° 斜角、底部横封、尾部加长），尺寸可参数放大（s×s 方块）
- [x] 输入系统完成：`input_task`（15ms 采样、死区 ±100、灵敏度换算 `(delta*sens)/512`、SW 边沿检测）+ 事件队列 `g_event_queue`（容量 8）+ `ui_task`（阻塞消费 → `cursor_move`）
- [x] 摇杆引脚迁移：PA2/PA3 → **PA4(ADC1_IN4) / PA5(ADC2_IN5) / PA6(SW)**。原因：PA0 被板载 WK_UP 占用（10k 上拉影响 ADC 读数）、PA1 接板载电位器；C 代码零改动（引脚全部走宏，CubeMX 重新生成自动更新）
- [x] **修复 postgen.ps1"等长替换"陷阱（重点 bug）**：替换串 41 字节 vs 原串 38 字节，多出的 3 字节覆盖了下一行的 `\r\n` 和 `#`，把 SRAM/TIM 两行合并成一条脏行 → `HAL_TIM_MODULE_ENABLED` 丢失（TIM6 是 HAL 基准时钟，SYS Timebase=TIM6）→ `TIM_HandleTypeDef` 未定义编译报错。修复：改为纯文本级替换 + 脏行自愈 + TIM 宏一并管理
- [x] freertos.c 中文注释被 CubeMX 按系统 ANSI 重写 → 乱码且无法自动还原；改为英文注释（此文件 CubeMX 每次生成都会重写，英文免疫编码问题；App/ 下自有代码保留中文）
- [x] 摇杆验证通过：光标移动方向正确、手感良好，SW 按键事件边沿检测就绪
- [x] 桌面框架 v1 完成：`desktop.c` 状态机（BOOT 启动画面 2 秒 → LOGIN 密码键盘 → DESKTOP 桌面图标），4 位密码（默认 1234）、错误提示、连续错 3 次锁定 30 秒（倒计时期间输入全部无效）；键盘高亮由光标位置驱动（局部重绘旧格+新格）
- [x] **修复 "cannot read project file"（重点 bug）**：加完登录画面后 Keil 打开工程报错、工程树为空，UV4 命令行编译退出码 15 且不写日志。排查：uvprojx/uvoptx 均通过 XML 解析、desktop.c 条目位置正确、无 Keil 进程占用——文件侧全部验证通过仍复现；最后 `git diff` 对照"编译通过的 HEAD 版本"，发现唯一可疑差异 = 工程文件第一行多了一个 UTF-8 BOM。剥离 BOM 后 UV4 命令行编译通过（0 Error / 0 Warning）。**根因：uVision4 5.24 的老解析器读不了带 BOM 的 uvprojx**。已固化进 postgen.ps1 2c 节兜底剥离
- [x] **修复光标白块覆盖 bug**：`cursor_erase` 用白色矩形填充整个光标外框来"擦除"，光标路过文字/图标/键盘格会留下白块。改为**无损恢复**：新增 `atk_md0280_read_area`（0x2E 读 GRAM，每像素 2 次读拼 RGB565）+ `atk_md0280_write_area`（0x2C 连续写）批量读写；光标移动时先把旧位置外框"整块读回→整块写回"恢复背景，再画新位置。缓冲固定 8 行 × 屏宽（3.75KB .bss），分块循环适配任意光标大小（1~8）
- [x] **发现并修复"所有中文不显示"bug**：`atk_md0280_show_string` 的循环条件是 `(*str >= ' ') && (*str <= '~')`——**库只支持 ASCII**，遇到中文直接退出不画。"请输入密码/密码错误/图标名"等全部从未显示过（"密码错误"只有白底擦除生效）。**决策：界面文案全部改英文**（Enter Password / Wrong Password / Locked Ns / Starting... / Files / Paint / Music / Settings），避免引入 GB2312 字库依赖
- [x] 顺带修正：hint 行擦除区 55→47（原擦到 y=52 起的密码圆点）；键盘格高亮重绘后 `cursor_show()` 重画光标（格子会盖住光标，保证光标永远最上层）；图标名按 strlen 动态居中

### 设计决策
- 屏幕渲染独占（单写者原则）：所有 LCD 绘制只在 `ui_task`，其他任务禁止碰屏幕，避免并发抢 FSMC 总线导致画面撕裂
- 事件驱动架构：`ui_task` 用 `xQueueReceive(..., portMAX_DELAY)` 永久阻塞，无事件时完全让出 CPU；生产（input_task）与消费（ui_task）通过队列解耦
- 固化 CubeMX 重生成流程：改 `.ioc` → GENERATE CODE → 双击 `postgen.bat` → 编译，任何一步异常先检查实际生成的文件内容，别只信"脚本看起来对"
- 引脚宏化设计兑现价值：换引脚只需改 CubeMX，C 代码零改动
- **文件编码铁律**：uvprojx 必须是**无 BOM 的 UTF-8**（Keil4 不认带 BOM 的工程文件）；hal_conf.h 等纯 ASCII 文件用 ASCII 编码写；只有带中文的源文件才用 UTF-8 带 BOM（Keil+VS Code 双兼容）。postgen 写入时必须显式确认编码，别想当然"UTF-8 就行"
- **界面文案用英文**：atk_md0280 库 show_string 只支持 ASCII，且正点原子库文件是 GBK 编码（不能往里写 UTF-8 中文，会混编码乱码）；界面文字统一英文 + 库改动注释全英文（同 freertos.c 免疫原则）
- **光标必须无损移动**：覆盖在界面内容上的浮动层（光标/弹窗）移动时不能"填背景色擦除"，必须读回保存-写回恢复（read_area/write_area），否则路过处留下痕迹

### 明日计划
- 烧录验证：BOOT 蓝屏 2 秒 → 英文登录界面输入 1234 → 桌面；错密码红字提示、连错 3 次锁定 30 秒；光标划过文字/图标/格子不再留白块
- 桌面图标 hover 高亮 + SW 点击打开应用（Files → 阶段 4）

## 2026-08-18

### 今日进度
- [x] **实验确定 GRAM 读取语义（重点 bug 后续）**：上一版 `read_area` 用"一次 0x2E 命令 + 连续读"，实测光标恢复后出现马赛克。用 `gram_probe` 实验函数读 12 个像素（用户读屏报数：46 63488 4 64516 0 63736 0 1276 63488 63488 63520 65472），数值明显错乱——**这块 ILI9341 类面板，一次 0x2E 后连续多像素读回的是坏数据**；只有"逐像素重新设窗口 + 0x2E + 虚读 + 2 次读拼 RGB565"（`read_point` 原样）可靠。`read_area` 改为循环调用 `read_point`（约 5ms/帧，正确性优先）；`gram_probe` 已删除
- [x] **光标重写为两段式"先存后画"（修复残影残留 bug）**：旧逻辑是"移动时先画到新位置、再读旧位置屏幕内容写回"——读屏读到的是**光标自己**，写回后把残影固化。改为：画光标**前**先把外框干净背景存进内存缓冲（`cursor_save_bg`），移动时 `cursor_restore_bg` **从缓冲写回**（绝不重新读屏），顺序固定"先恢复旧位置 → 再存新位置 → 再画新光标"（覆盖重叠时恢复完新位置区域也干净了）。缓冲 72×80×2=11.25KB 静态分配，光标大小上限钳到 4
- [x] **修复数字框边光标残影（重点 bug）**：快速滑动手柄时新格高亮重绘 `fill` 只盖住格子 60×45 区域，而光标外框 54×60 上下溢出格子约 8 像素——fill 把光标"腰斩"，随后 `cursor_show` 把"截断光标"当背景存进缓冲，下次恢复固化到框边。修复：新增 `cursor_hide()`（把已存背景写回屏幕，光标消失）+ `cursor_overlap()`（矩形相交判断）；desktop 所有局部重绘（格子高亮、提示行、密码圆点、锁定倒计时）统一走 `redraw_protect_begin/end`——与光标外框相交时先隐藏、重绘完再画回，保证"存背景"永远发生在屏幕干净时；不相交则零开销直画
- [x] **修复密码圆点"蓝心残留"（擦除不彻底 bug）**：圆点从未输入"蓝色实心 7×7"改成"灰色空心框"时只画了 1px 边框，中间 5×5 蓝色残留，退格/输错清空后看着像还输着密码。修复：画空框前先 `fill` 白底整块擦除

### 设计决策
- 光标背景恢复的唯一正确姿势：**内存缓冲两段式**（先存干净背景再画，恢复用缓冲不用读屏）。"读屏恢复"在任何时序下都可能读到光标自己或截断光标，一律不用
- 浮动层（光标）与局部重绘共存的安全协议：**重绘前 hide、重绘后 show**，并配矩形相交判断——相交才保护（键盘高亮几乎每次），不相交零开销（提示行/圆点大部分时候）
- 局部重绘"擦除不彻底"自查清单：从"实心"切到"空心/框"的状态切换，必须先整块填底色再画细节（圆点、高亮格、进度条同理）
- 性能账：光标每帧成本 ≈ 恢复(写) + 保存(读 3240 点 ≈5ms) + 绘制；高亮切换帧额外一次 hide/show。事件 15ms 一帧，正确性优先，卡顿后再优化（如读窗口裁剪）

### 明日计划
- 桌面图标 hover 高亮 + SW 点击打开应用（Files → 阶段 4）
- 任务栈水位检查（uxTaskGetStackHighWaterMark，阶段 1 遗留项）
